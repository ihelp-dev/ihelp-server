# Do not change version. This is the version of aws buildspec, not the version of your buldspec file.
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10

  pre_build:
    commands:
      - aws --version
      - node --version
      - docker --version
      - NODE_ENV=$NODE_ENV
      - echo node port
      - NODE_PORT=$NODE_PORT
      - echo image uri
      - echo ${IMAGE_URI}
      - IMAGE_TAG=latest
      - echo Image tag
      - echo ${IMAGE_TAG}
      - echo node image uri
      - echo ${NODE_URI}
      - echo Logging in to Amazon ECR...
      - $(aws ecr get-login --no-include-email --region $AWS_REGION)
      - echo Installing Mocha...
      - npm install -g mocha
      #- npm run test
  build:
    commands:
      - echo Build started on `date`
      - echo Building images...
      - echo "Current working directory"
      - pwd
      - docker build . -t ${IMAGE_URI}:${IMAGE_TAG} --build-arg NODE_IMAGE=${NODE_URI}:latest --build-arg NODE_PORT=${NODE_PORT}
      - echo Tag the docker image
      - docker tag ${NODE_URI}:latest ${IMAGE_URI}:${IMAGE_TAG}

  post_build:
    commands:
      - echo Build completed on `date`
      - echo Push image to ECR
      - docker push ${IMAGE_URI}:${IMAGE_TAG}
      - printf '[{"name":"ihelp-server","imageUri":"%s"}]' ${IMAGE_URI}:${IMAGE_TAG} > imagedefinitions.json
      - cat imagedefinitions.json

# Include only the files required for your application to run.
artifacts:
  files:
    - imagedefinitions.json